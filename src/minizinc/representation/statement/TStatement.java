package minizinc.representation.statement;

/**
 * 
 * @author rafa Categories of statements allowed in a MiniZinc model
 *
 */
public enum TStatement {
	TABLE, CONSTRAINT, VARDECL, PARDECL, SOLVE, OUTPUT, PREDICATE, FUNCTION, INCLUDE, INIT, COMMENT
}
